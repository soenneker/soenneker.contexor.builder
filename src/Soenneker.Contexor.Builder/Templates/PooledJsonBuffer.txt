// <auto-generated/>
#nullable enable
using System;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Text.Json.Serialization.Metadata;
namespace @@NAMESPACE@@;

// UTF-8 scratch storage is rented only for the duration of serialization or a transport write.
internal sealed class PooledJsonBuffer : global::System.Buffers.IBufferWriter<byte>, global::System.IDisposable
{
    private byte[] _buffer = global::System.Buffers.ArrayPool<byte>.Shared.Rent(512);
    private int _written;
    internal global::System.ReadOnlyMemory<byte> WrittenMemory => _buffer.AsMemory(0, _written);
    public void Advance(int count)
    {
        if (count < 0 || count > _buffer.Length - _written) throw new global::System.ArgumentOutOfRangeException(nameof(count));
        _written += count;
    }
    public global::System.Memory<byte> GetMemory(int sizeHint = 0) { Ensure(sizeHint); return _buffer.AsMemory(_written); }
    public global::System.Span<byte> GetSpan(int sizeHint = 0) { Ensure(sizeHint); return _buffer.AsSpan(_written); }
    private void Ensure(int sizeHint)
    {
        if (sizeHint < 0) throw new global::System.ArgumentOutOfRangeException(nameof(sizeHint));
        int required = checked(_written + global::System.Math.Max(sizeHint, 1));
        if (required <= _buffer.Length) return;
        byte[] next = global::System.Buffers.ArrayPool<byte>.Shared.Rent(global::System.Math.Max(required, checked(_buffer.Length * 2)));
        _buffer.AsSpan(0, _written).CopyTo(next);
        global::System.Buffers.ArrayPool<byte>.Shared.Return(_buffer);
        _buffer = next;
    }
    public void Dispose()
    {
        byte[] buffer = _buffer;
        _buffer = [];
        _written = 0;
        if (buffer.Length != 0) global::System.Buffers.ArrayPool<byte>.Shared.Return(buffer);
    }
}
